<?xml version="1.0" encoding="UTF-8"?><beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"       xmlns:context="http://www.springframework.org/schema/context"       xmlns="http://www.springframework.org/schema/beans" xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd     http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd http://code.alibabatech.com/schema/dubbo http://code.alibabatech.com/schema/dubbo/dubbo.xsd">    <!-- 激活annotation功能-->    <context:annotation-config/>    <!-- 自动扫描 -->    <context:component-scan base-package="com.spring"/>    <import resource="classpath:beans/*.xml"/>    <!-- =============== dubbo 服务 ================== -->    <!-- 应用名 -->    <dubbo:application name="alia_mvc_practice"/>    <!-- 监控 -->    <dubbo:monitor protocol="registry"/>    <!-- 注册中心 -->    <dubbo:registry address="${dubbo_registry_address}"/>    <!-- dubbo协议在20880端口暴露服务 -->    <dubbo:protocol name="dubbo" port="${dubbo_protocol_port}"/>    <!-- 声明需要暴露的服务接口 -->    <dubbo:service timeout="${dubbo_service_timeout}" interface="com.com.epcc.recon.taskmanager.facade.ClearCompleteNotifyService" ref="clearCompleteNotifyService" version="1.0.0"/>    <!-- 声明需要引用的服务接口 -->    <dubbo:reference timeout="${dubbo_control_service_timeout}" id="baseService" check="false" interface="com.com.epcc.control.basedata.facade.service.BaseService" version="${dubbo_control_version}"/></beans>